Create Database University;

Use University;

Create Table Student(rollno int Primary Key,
                     Name varchar(30),
                     Class varchar(30),
                     Contact varchar(30));

Create Table Professor(ProfID int Primary Key,
                        )